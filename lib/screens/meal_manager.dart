import 'package:flutter/material.dart';

class MealManagerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Meal Manager')),
      body: Center(child: Text('Meal Manager Dashboard')),
    );
  }
}
